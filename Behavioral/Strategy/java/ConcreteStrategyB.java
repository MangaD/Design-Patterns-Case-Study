/**
 * @brief Another Concrete Strategy that implements a different algorithm.
 */
public class ConcreteStrategyB implements Strategy {
	@Override
	public String execute() {
		return "Executing strategy B.";
	}
}
